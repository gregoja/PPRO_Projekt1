package cz.uhk.ppro.projekt.repository;
/*
import cz.uhk.ppro.projekt.entity.Product;
import org.springframework.data.domain.Pageable;
import org.springframework.data.domain.Sort;
import org.springframework.data.repository.PagingAndSortingRepository;

import java.util.List;

public interface ProductRepositoryDisable extends PagingAndSortingRepository<Product,Long> {
    List<Product> findAll(Sort sortOrder);
    List<Product> findAllByProductCategoriesByCategoryId_Name(String categoryName, Pageable paging);
    //List<Product> findAllByProductCategoriesByCategoryId(ProductCategory category, Pageable paging);
}*/