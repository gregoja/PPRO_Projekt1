package cz.uhk.ppro.projekt.web;
/*
import cz.uhk.ppro.projekt.entity.Student;
import cz.uhk.ppro.projekt.service.StudentService;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RestController;

import java.util.Optional;

@RestController
public class StudentController {
    private StudentService service;

    public StudentController(StudentService service) {
        this.service = service;
        //service.save(new Student("Jan","Novák"));
        //service.save(new Student("Jana","Nováková"));
    }

    @GetMapping("student/{id}")
    public Optional<Student> getById(@PathVariable long id) {
        // melo by spis vratit 404
        return service.getById(id);
    }
}*/